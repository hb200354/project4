<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gomonth.dao.UserDAO">

    <select id="loginCheck" resultType="com.gomonth.dto.UserDTO">
        SELECT 
            user_id AS userId, 
            user_pw AS userPw, 
            user_name AS userName, 
            email
        FROM USERS
        WHERE user_id = #{userId} AND user_pw = #{userPw}
    </select>

    <insert id="insertUser" parameterType="com.gomonth.dto.UserDTO">
        INSERT INTO USERS (user_id, user_pw, user_name, email)
        VALUES (#{userId}, #{userPw}, #{userName}, #{email})
    </insert>

</mapper>